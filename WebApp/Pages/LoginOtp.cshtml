@page
@model SEGEDE_Grupo1.WebApp.Pages.LoginOtpModel
@{
    ViewData["Title"] = "Verificación OTP";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>@ViewData["Title"]</h2>
    </div>
        <div class="row justify-content-center">
        <div class="col-md-6 col-lg-5">
            <div class="card shadow-sm">
                <div class="card-body p-4 text-center">
                    <h5 class="card-title mb-3">Autenticación de Dos Factores</h5>
                    <p class="text-muted small">Ingresa el código de 6 dígitos enviado a tu correo electrónico.</p>
                    <div class="alert alert-info py-2 small mb-3 d-flex justify-content-between align-items-center">
                        <span>El código expira en:</span>
                        <strong id="loginOtpCountdown">03:00</strong>
                    </div>
                    <form id="otpForm">
                        <div class="mb-3">
                            <input type="text" class="form-control text-center fs-3 letter-spacing" id="otpCode" maxlength="6" placeholder="000000" required>
                        </div>
                        <div class="d-grid mb-3">
                            <button type="submit" class="btn btn-primary">Verificar e Iniciar Sesión</button>
                        </div>
                        <div class="d-grid">
                            <button type="button" class="btn btn-outline-primary btn-sm" id="resendOtpBtn">
                                <i class="bi bi-arrow-repeat" aria-hidden="true"></i> Reenviar código
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/pages-controller/LoginViewController.js" asp-append-version="true"></script>
}