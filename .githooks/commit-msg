#!/bin/sh

commit_message_file="$1"

if grep -Eiq '^Co-authored-by:.*(chatgpt|codex|openai)' "$commit_message_file"; then
    echo "Error: no se permiten trailers Co-authored-by atribuidos a ChatGPT, Codex u OpenAI." >&2
    exit 1
fi
